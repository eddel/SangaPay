import { AppPlaceholderPage } from "@/components/sangapay/app-placeholder-page";

export default function NotificationsPage() {
  return (
    <AppPlaceholderPage
      title="Notifications"
      summary="Approval alerts, rate triggers, and settlement updates will live here in a later milestone. The inbox route exists now so the top-bar control is a real destination."
    />
  );
}
