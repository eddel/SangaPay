import { AppPlaceholderPage } from "@/components/sangapay/app-placeholder-page";

export default function RatesPage() {
  return (
    <AppPlaceholderPage
      activeTab="rates"
      title="Rates"
      summary="Live FX history and detailed pricing land here in a later milestone. For now, this route keeps dashboard navigation fully real and testable."
    />
  );
}
